import styled from "styled-components";

const Head = styled.header`
  padding: 1rem 0;
`;

const Title = styled.h2`
  color: #2e3b52;
  font-weight: 500;
  font-size: 24px;
  line-height: 29px;
`;

export { Head, Title };
