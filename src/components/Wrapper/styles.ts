import styled from "styled-components";

const Wrap = styled.div`
  display: flex;
  min-height: 100vh;
`;

const Main = styled.main`
  width: calc(100% - 220px);
  padding: 0 35px;
  position: relative;
`;

export { Wrap, Main };
